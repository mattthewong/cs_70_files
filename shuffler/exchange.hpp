#ifndef EXCHANGE_HPP_INCLUDED
#define EXCHANGE_HPP_INCLUDED 1

#include <vector>
#include <string>

/*declared the function exchange in the .hpp file.
Exchange takes in two strings, and swaps the value of one with the other.
*/

void exchange(std::string& s1, std::string& s2); //included & so declaration in .hpp matches implementation in .cpp.

#endif